# Audit Log

Author: Richard Van Winkle, Matrikelnummer: 3214183

| ID | File | Problem / manifestation | Mathematical or logical root cause | Fix implemented | Commit hash |
|----|------|-------------------------|------------------------------------|-----------------|-------------|
| fix1 | data.py | Cannot read the data files, runtime error | There is a _data suffix after the file name ln. 11 | delete the suffix in code, so actual file names are brought in | 0263575 |
| fix2 | models.py | the first input to nn.crossEntropyLoss() was noneType, not Tensor | the forward() call in the ResNet18 class was not returning the images, automically assigning them to none | set self.classifer(out) to out, then returned out | 0255111 |
| fix3 | data.py | the target parameter to nn.crossEntropyLoss was the wrong shape | If first input is shape (N,C) the target input must be of shape (N), it was (N, 1) | used torch.squeeze(-1) object to shape the target correctly | ba4fc39 |
| fix3 | data.py | training and validation splits overlapped | training smaple size was the same as total sample size, not a 90/10 split | seperated training data using the val_split variable | ba4fc39 |
| fix4 | fit.py | train and val loss exploded in value as the epochs increased | The gradients were not getting zeroed after each pass through the NN, causing the weights to reuse/accumulate gradients and wander aimlessly, i.e. to not optimize | I added the optimizer.zero_grad() function right before the self.model call | 62b2833 |
| fix5 | models.py | ResNet18 training accuracy was around 66 - 68% | ResNEt18 model was using Identity() as the activation function, so there was no non-linear activation. The ResNet paper says identity refers to the shortcut connection and that ReLU is used for activation | changed activation_str to ReLU, validation accuracy improved to 75.3% | 78c1d44 |
| fix6 | train.py, fit.py, test.py | no independent test/evaluation pipeline existed and trained models were not saved | the recovered pipeline could train but did not preserve the best model or compute required test metrics for the report | saved the best validation checkpoint during training and added test.py to load it, compute accuracy, precision, recall, macro F1, latency, and memory, then append results to benchmark_results.txt | 945745d |
| fix7 | models.py | VGG16 crashed with a channel mismatch inside VGGBlock | VGGBlock did not update current_in_channels after each convolution, so later convolutions expected the original input channels instead of the previous layer's output channels | updated current_in_channels to out_channels after each convolution layer is added | a66507c |
| fix8 | models.py | VGG16 crashed at the first Linear classifier layer with `mat1 and mat2 shapes cannot be multiplied (8x4608 and 2048x1024)` | the VGG feature extractor produced a flattened feature vector of size 4608, but the classifier was hard-coded to expect 2048 input features | added `nn.AdaptiveAvgPool2d((2, 2))` before flattening so VGG16 always passes a fixed 2048-feature vector into the classifier | 234281e |
| fix9 | models.py | AlexNet crashed at the classifier with `mat1 and mat2 shapes cannot be multiplied (8x3072 and 2048x1024)` | AlexNet had hard-coded assumptions about input channels, classifier input size, and output classes | changed AlexNet to use configured `in_channels` and `num_classes`, and added adaptive pooling so the classifier receives a fixed 768-feature input | 866f9d9 |
| fix10 | train.py, test.py | benchmark results did not include all required efficiency metrics | the evaluation output logged accuracy, precision, recall, macro F1, and inference latency, but did not clearly record total training runtime, training peak memory, and inference peak memory as separate values | added training runtime and peak training memory to the saved checkpoint in train.py, then updated test.py to append training runtime, training peak memory, inference latency, and inference peak memory to benchmark_results.txt | aeb4247 |
| fix11 | train.py, test.py | AlexNet and VGG16 were training, but the accuracy was much lower than it should have been | `drop_rate=0.99` was hard-coded when creating the models, so AlexNet and VGG16 dropped 99% of classifier activations during training. ResNet18 was not affected because it does not use dropout | removed the hard-coded dropout override so AlexNet and VGG16 use their default dropout value | ba9d0f4 |
| fix12 | models.py | the Green Initiative required an actual reduced-complexity architecture, not only a comparison of existing models | AlexNet was the fastest architecture, but its classifier still used two large 1024-unit fully connected layers, adding unnecessary parameters and compute | reduced the AlexNet classifier from `768 -> 1024 -> 1024 -> num_classes` to `768 -> 256 -> 128 -> num_classes` while keeping the convolutional feature extractor unchanged | 57a34df |
| fix13 | train.py, test.py | the `organs` dataset only had scratch-training benchmarks, but the assignment required a knowledge-transfer comparison | `organs` is a low-sample dataset, while `orgs` is a larger grayscale 11-class dataset with matching input/output structure, so `orgs` ResNet18 weights can be used as transferred initialization | added a config-driven `TRANSFER_FROM` option that loads the source checkpoint before training and saves/tests a separate transfer checkpoint | 1d24bee |